export interface contrato {
    id?: number; 
    description: string;
    salary: number;
    charge: string;
    startDate: string;
    endDate: string;
    eps: string;
    contractType: string; 
    candidateId?: number;
}