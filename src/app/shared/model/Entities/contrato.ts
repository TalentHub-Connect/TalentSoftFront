export interface contrato {
    id?: number; 
    salary?: number;
    startdate: string;
    contract_type: string; 
    enddate: string;
    eps: string;
    charge: string;
    candidate_id?: string;
}